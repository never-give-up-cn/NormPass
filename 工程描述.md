整套网站【中文密码 + UTF-8 标准存储 + 登录可选编码兼容】完整工程逻辑
先定全局顶层规约，再拆分注册流程、登录流程、异常分支、数据层、前端交互、边界约束、安全规则、日志规范，完整串联整个系统。
前置全局约定（不可变更核心基线）
密码存储唯一标准（注册 / 重置密码强制生效）
所有账号创建、密码重置时，统一执行一套固定流程：
用户输入明文字符串 → Unicode NFC 标准化 → 使用 UTF-8 严格编码成二进制字节 → 使用 Argon2id/bcrypt 进行密码哈希运算
数据库每条用户记录仅保存一条哈希值，永远不存在多条哈希，不存储明文密码。
核心意义：系统只认一套标准基准，兼容逻辑只放在登录阶段，不污染数据存储层。
编码定位
UTF-8 = 原生标准，默认选项，完整支持全部简体、繁体、生僻汉字、Emoji、特殊符号；
GBK、GB2312、UTF-16LE = 仅作为登录兼容备选，仅用来适配极少数老旧客户端编码错乱场景。
编码白名单
后端硬编码允许的编码集合：utf-8、gbk、gb2312、utf-16le。前端下拉框只能输出这四个值；任何外部请求传入不在名单内的编码，直接拒绝。
统一预处理规则
无论注册还是登录，拿到用户输入密码文本，第一步必须执行 unicodedata.normalize("NFC")
解决视觉相同但 Unicode 码点不同字符（组合音标、异体字符、不同编码形态文字）导致的 “看着一样，校验失败”。
转码策略：errors="strict"
字符无法使用选定编码表达时，直接抛出异常，禁止系统自动替换为�占位符；防止出现不可复现的诡异登录结果。
第一部分：用户注册完整执行链路
输入
前端表单：用户名、明文密码（支持中文）
注册页面没有编码选择下拉框，编码规则强制锁定，不给用户选择。
后端串行步骤
接收 HTTP 请求 JSON 数据，强制使用Content-Type: application/json;charset=utf-8
取出原始密码字符串 raw_password
执行标准化：norm_str = unicodedata.normalize("NFC", raw_password)
强制使用 UTF-8 进行二进制编码
pwd_bytes = norm_str.encode("utf-8", errors="strict")
极端场景：如果 UTF-8 编码失败（理论几乎不可能，UTF-8 可以编码全部 Unicode 字符），返回注册失败。
使用密码哈希算法（bcrypt/Argon2id）基于pwd_bytes生成哈希摘要
将【用户名 + 密码哈希】写入用户数据表
返回注册成功，引导跳转登录页
数据表关键结构简化
plaintext
user(
    id bigint primary key,
    username varchar unique,
    password_hash text, -- 仅此一列存放密码哈希，无其他扩展字段
    ...其他业务字段
)
注册约束补充
重置密码页面逻辑和注册完全一致：无编码选择，新密码统一按照 NFC+UTF-8 规则生成哈希。
长期策略：新注册用户全部标准 UTF-8，逐步降低老旧编码兼容场景依赖。
第二部分：用户登录完整执行链路（整套工程最复杂分支）
输入
前端表单：明文密码 + 用户手动选择的编码（下拉选择，默认选中 UTF-8）
后端串行处理步骤
接收参数：password 用户输入文本、encoding 用户选择编码名称
安全校验：判断encoding是否存在于后端编码白名单
不在白名单 → 返回错误码：不支持的编码格式，终止流程
密码文本统一预处理：
norm_str = unicodedata.normalize("NFC", raw_password)
尝试使用用户选中编码，转换字符串为二进制字节
python
运行
try:
    input_bytes = norm_str.encode(encoding=encoding, errors="strict")
except UnicodeEncodeError:
    # 分支A：编码转换失败
    返回业务错误码【2001】
    含义：当前输入的字符集合，无法使用你选中的编码表达
分支 A：捕获 UnicodeEncodeError（编码失败场景）
前端收到错误码后展示专属提示文案：
当前选择【GBK】编码无法支持你输入的文字（生僻字 / 特殊符号 / Emoji）
请切换编码为 UTF-8 重试；多次登录异常，可以点击按钮前往重置密码
重点区分：这个失败 ≠ 密码错误。
用户密码本身可能正确，仅仅是所选编码承载不了输入文字，不能统一提示 “密码错误” 造成误导。
分支 B：编码成功，得到 input_bytes
拿着input_bytes和数据库读取出来的password_hash执行哈希校验：
校验成功（bcrypt/argon 匹配）：返回成功码 0，创建会话，登录放行
校验失败：返回错误码【1001】，提示：密码输入错误
关键逻辑澄清（极易产生理解误区）
数据库里的哈希，永远是【文本 → NFC → UTF-8 字节】算出来的。
如果用户登录选择 GBK：
系统操作是：用户输入文本 → NFC → GBK 字节，拿着 GBK 字节去比对UTF-8 字节生成的哈希。
👉 这套兼容机制只能修复一类特定故障：
早期客户端错误传输原始 UTF8 字节流、并错误当成 GBK 文本解析回传给服务器的历史兼容场景。
它不能实现 “注册用 GBK 存、登录用 UTF8 解锁”，存储基准自始至终唯一。
第三部分：前端交互完整逻辑
登录页面控件
密码输入框
编码下拉选择框（默认 UTF-8）
消息提示区域
初始状态：提示区域隐藏
点击登录触发请求，等待后端响应，按错误码分流渲染文案：
0：登录成功，跳转主页
1001：密码错误
2001：所选编码不兼容文字，附带【重置密码】超链接
4000：非法编码参数
UI 友好优化
下拉框增加备注：GBK/GB2312 不支持生僻字、表情符号；优先使用 UTF-8。
注册页面、重置密码页面：移除编码下拉组件，消除用户混淆。
第四部分：限流、安全与风控逻辑（工程生产必备）
登录失败限流
同一个账号，连续多次登录失败（例如 5 次），临时锁定账号一段时间（10 分钟）
目的：防止自动化脚本循环遍历「GBK/UTF8/GB2312」组合暴力破解密码。
请求参数校验
禁止前端自由传递任意编码字符串，后端强制白名单拦截，抵御参数注入。
日志分层记录（便于后期运维分析）
每次登录请求落地日志，区分三类结果：
① 登录成功
② 编码转换异常（错误码 2001）
③ 哈希比对不匹配（密码错误 1001）
运维可以统计：有多少用户依赖 GBK 等老旧编码登录，未来评估是否可以移除兼容选项。
明文安全约束
前后端传输密码使用 HTTPS；服务端内存处理完成后，尽可能及时清理明文变量；任何场景禁止记录明文密码到日志。
第五部分：各类典型场景运行演示，直观理解逻辑
场景 1：常规用户，密码普通中文，登录使用默认 UTF-8
注册：文字 → NFC → UTF8 字节 → 哈希入库
登录：输入同样文字，选 UTF8 → NFC→UTF8 字节 → 哈希匹配 → 登录成功
场景 2：密码包含生僻汉字「䂙」，登录错误选择 GBK
norm_str.encode ("gbk",strict) → 抛出 UnicodeEncodeError
返回 2001 错误，前端提示切换 UTF-8，提供重置入口。
场景 3：历史老旧客户端导致编码错乱的存量用户
用户原始密码登录时选择 UTF8 一直失败，手动切换 GBK 后，生成字节刚好和库内哈希匹配 → 登录成功。
场景 4：密码含有 emoji，登录选择 GBK
GBK 无法编码 Emoji → 编码异常，提示切换 UTF-8。
场景 5：用户简繁输入错误（「后」和「後」）
NFC 标准化不会合并简繁异体字，两个字符码位不同，最终字节不同，校验失败，提示密码错误。文案提前告知用户区分简繁体。
第六部分：系统局限性文档（需要在需求文档写明）
兼容编码（GBK/GB2312）仅用于历史场景兼容，不作为长期特性；
该方案无法解决「注册时客户端编码错乱」，只能兼容登录阶段的编码适配；
GBK 本身字符集有限，无法覆盖全部 Unicode 字符，带有生僻字的密码只能使用 UTF8 登录；
无法消除 Unicode 异体字符问题，依赖 NFC 归一化缓解，不能 100% 解决所有视觉同形字符。
第七部分：演进路线（可选工程规划）
长期运营方案：
持续引导遇到编码兼容问题的用户执行密码重置；重置后的账号虽然底层规则不变，但用户使用 UTF8 正常登录，不再需要切换老旧编码。
当日志统计到极少用户使用 GBK 兼容模式后，可以在下一次版本迭代直接移除登录下拉框，彻底简化系统，只保留统一 UTF8 标准。